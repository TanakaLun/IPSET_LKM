# IPSET_LKM
Allows you to use IP-SET without actually modifying the kernel image.
- For KernelSU users, you can directly flash the module without any manual pre-installation. For non-KernelSU users, you need to ensure that a callable ksud binary exists in your executable's path.